export interface cards {
    title: string;
    total: number;
    percentage: number;
    desc: string;
    isIncreasing: boolean;
    icon: string;
}

export interface student {
    id: number;
    name: string;
    email: string;
    phone: string;
    instance: string;
    createdAt: string;
}